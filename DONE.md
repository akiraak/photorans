# DONE

## 2026-05

- 2026-05-04: 未分類タブが空に見えるバグの修正 + Picker 構造分離 ([plan archive](docs/plans/archive/unclassified-segment-empty-bug.md)) — 「Group 詳細で `[未分類|グループ]` Picker の `未分類` をタップするとリストが空になる」バグ (`HomeQueries.directItems` が `.group(X)` scope で `X.items` を返していたため、`group == nil` の Item と無関係な Group X 直下 Item にすり替わっていた) を `v0.1.24` で修正。Akira さん仕様確定: 「Group X の 未分類」という中間概念は存在せず、`未分類` は常に `group == nil` のアイテム集合を指すグローバルなモードフィルタ。Picker 自体も `HomeView` 内に置かれて階層 push でリストと一緒にスライドしていたのを、`RootView` 直下 (NavigationStack の外側) に固定する構造改修も同時に実施。Phase 1 で 2 状態 (`@State path` グループモード階層用 / `@State unclassifiedPath` 未分類モード Item 詳細用) + `@State selectedSegment` を `RootView` に集約、`ZStack + opacity + allowsHitTesting` で 2 本の NavigationStack を identity 維持して切替 (`if/else` で出し入れすると path が消えるため不採用)、未分類モード側にも独立 NavigationStack を 1 本立てて `navigationDestination(for: Item.self)` を宣言 (`NavigationLink(value:)` は祖先 NavigationStack 必須、ZStack 直下に裸で置くと Item 詳細遷移が壊れる)。`HomeView` から Picker 描画と `@State selectedSegment` を撤去しグループモード専用に簡略化、`UnclassifiedListView` は scope 引数を削除し `@Query(filter: #Predicate<Item> { $0.group == nil }, sort: \.createdAt, order: .reverse)` で取得 (PoC で懸念した optional to-one nil 比較は iOS 26 SDK + SwiftData で動作)、`HomeFAB(scope: .root)` overlay も追加して未分類モードからの撮影 (`group == nil` で保存) を維持。`HomeQueries.filterItems` / `directItems` は唯一の呼び出し元が `@Query` フィルタに移行したため関数ごと削除、グループモード Group X の混在表示用に `enum HomeRowEntry { case group(ItemGroup); case item(Item) }` と `directContents(group:) -> [HomeRowEntry]` (`X.children` と `X.items` を `createdAt` 降順マージ) を追加、`GroupListView` は Root scope (`parent == nil` の Group のみ) は無変更で Group X scope 時に `switch entry` で Item 行 (`ItemRowView` + `NavigationLink(value: Item)`) と Group 行 (既存 `rowView(for:)` + `NavigationLink(value: ItemGroup)`) を分岐レンダリング (Akira さん仕様: 子 Group と子 Item を 1 リストに混在、セクション分けしない)。`SegmentScope.defaultSegment` も Picker 状態がグローバル化して scope ベース既定値が無意味になったため削除、`SegmentQueryTests` は `filterItems` 系 2 ケースを削除し `directContents(group:)` 用 4 ケース (混在 createdAt 降順 / Group のみ / Item のみ / 両方空) を追加。Phase 2 Step 2.1 (ローカル / シミュレータ確認) は WSL2 で実行不可のため飛ばし、`v0.1.24` として TestFlight 配布、Akira さん実機確認 OK (2026-05-04): Picker が階層 push でアニメーションせず固定 / 未分類モードは Root でも Group 詳細からでも `group == nil` の Item が並ぶ / Item 詳細遷移 + 状態保持 (グループモード `path` / 未分類モード `unclassifiedPath` の両方が `ZStack + opacity` で破棄されない) / 撮影 FAB の保存先解決 (未分類モード → `nil` / Group X → `X`) すべて期待どおり
- 2026-05-04: アイテムとグループの一覧に画像を表示する ([plan archive](docs/plans/archive/list-thumbnails.md)) — ホーム画面 (`HomeView`) の 2 セグメント (`未分類` / `グループ`) のリスト行にサムネを表示する機能を `v0.1.23` で実装。Step1 で `ThumbnailCache` (`NSCache<NSString, UIImage>` を `final class ... : @unchecked Sendable` + `static let shared` で包んだ薄いラッパ、`countLimit = 200`、キーは `"<imagePath>@<intW>x<intH>"`) を新設、I/O や縮小は持たず純粋にキー → UIImage の保管のみとし、生成側 (`ItemThumbnailView`) と責務を分離。明示的な無効化 API は提供せず、Item / Group 削除時は次回参照で `UIImage(contentsOfFile:)` が nil を返した結果プレースホルダに切り替わる設計 (NSCache のメモリ警告 evict に任せる)。Step2 で `ItemThumbnailView(imagePath:size:)` を新設、`.task(id: imagePath)` で `ThumbnailCache.shared.cached(...)` を確認 → なければ `Task.detached(priority: .userInitiated)` 内で `UIImage(contentsOfFile:)?.preparingThumbnail(of: targetSize)` を呼び、結果を MainActor で `@State image` にセット + `cache.store`。サムネは `Image(uiImage:).resizable().aspectRatio(.fill).frame(...).clipped().clipShape(RoundedRectangle(cornerRadius: 6))` で 56pt 正方形クロップ、未取得 / 不在時は `RoundedRectangle.fill(.secondary.opacity(0.15)) + overlay(Image(systemName: "photo"))` プレースホルダ、行ラベルは外側で読めるので `.accessibilityHidden(true)`。`Task.isCancelled` ガードで path 切り替え時の古い結果反映を防止。Step3 で `ItemRowView` を `body` 直下で `HStack(alignment: .top, spacing: 12) { ItemThumbnailView(...); content }` に再構成、`content` は `@ViewBuilder` で `.processing` / `.completed` / `.failed` の 3 分岐 (旧 `processingRow` / `completedRow` / `failedRow` は `processingContent` / `completedContent` / `failedContent` にリネームし HStack 外側に統合)、`.failed` の右端リトライボタン位置とシマー overlay は維持。Step4 で `HomeQueries.representativeItem(of: ItemGroup) -> Item?` を `group.items.max(by: { $0.createdAt < $1.createdAt })` で実装 (子孫不再帰、`sortDirectGroups` の並び順「直下 Item の最新 createdAt 降順」と一貫させる)、`SegmentQueryTests` に 4 ケース (Item ゼロ → nil / Item 1 件 → その Item / Item 複数 → 最新 / 子孫 Group のみ持つ中間 Group → 直下のみで子孫不再帰) を追記。Step5 で `GroupListView.rowView(for:)` の leading icon を `representativeItem` ありなら `ItemThumbnailView`、なしなら `folderPlaceholder` (RoundedRectangle 56pt + `folder.fill` overlay) に分岐、サイズを Item 行と統一 (旧 `Image(systemName: "folder.fill").frame(width: 32)` は撤去)。Step6 で `cd ios && xcodegen generate` を実行し `Photorans.xcodeproj/project.pbxproj` に新規 2 ファイル (`ThumbnailCache.swift` / `ItemThumbnailView.swift`) を登録 (memory `feedback_xcodegen_regenerate.md` に従い同 commit に含める)。Step7 で `v0.1.23` として TestFlight 配布、Akira さん実機確認 OK (2026-05-04)。後追い検討: サブグループのみ持つ中間 Group の代表サムネは現状 folder アイコン据え置き (子孫再帰は体感悪化が出てから)、撮影時の事前サムネ生成は低速機でカクついたら検討
- 2026-05-04: アプリアイコンの再差し替え (`photorans-icon.png` 版) ([plan archive](docs/plans/archive/app-icon-replace-2.md)) — `v0.1.20` で配布した `photorans.png` 版を Akira さん判断で再差し替え。新素材 `/home/ubuntu/photorans/photorans-icon.png` (1254×1254 / 8-bit RGB / no-alpha) を Pillow で 1024×1024 / sRGB / RGB にリサイズし `ios/Photorans/Resources/Assets.xcassets/AppIcon.appiconset/icon-1024.png` を上書き、`Contents.json` は無変更、ルート直下の `photorans-icon.png` は反映後に削除。`.swift` 追加削除無しのため XcodeGen 再生成は不要。`v0.1.22` として TestFlight 配布、Akira さんが実機 (ホーム画面 / Spotlight / 設定アプリ) で新アイコンを確認 OK (2026-05-04)
- 2026-05-04: カメラ画面に閉じるボタンを追加 ([plan archive](docs/plans/archive/camera-close-button.md)) — `CameraView` (`fullScreenCover` 提示で edge swipe による dismiss が効かず、撮影成功時の `onCaptured` 以外に抜け道が無かった) に明示的な X ボタンを追加。`onClose: @MainActor () -> Void = {}` を `onCaptured` と並列で `CameraView` に追加し、`previewSection` ZStack の `.topLeading` (zoomHUD と同列) に SF Symbol `xmark` を `Circle().fill(.black.opacity(0.5))` 半透明黒丸背景で配置、44pt ヒット領域 + `accessibilityLabel("閉じる")` を付与。`HomeFAB` から `onClose: { isShowingCamera = false }` を配線。`isCapturing` 中も閉じる操作を許可 (進行中の撮影は capture delegate が完走するので Item 保存は走る) し、`permissionDeniedOverlay` 表示中も最前面で押せる位置に配置。`CameraViewModel` / `CameraSession` / `CameraPreviewView` / `Info.plist` (portrait lock + preview rotation 90° 固定) は無変更、`.swift` 追加削除無しのため XcodeGen 再生成はスキップ判断。`v0.1.21` として TestFlight 配布、Akira さんが実機で 撮影前 / 撮影中 / 権限拒否中いずれからも閉じられることを確認 OK (2026-05-04)
- 2026-05-03: アプリアイコン差し替え (`photorans.png` 版) ([plan archive](docs/plans/archive/app-icon-replace.md)) — `/home/ubuntu/photorans/photorans.png` (1254×1254 / RGB / no-alpha) を Pillow で 1024×1024 にリサイズし `ios/Photorans/Resources/Assets.xcassets/AppIcon.appiconset/icon-1024.png` を上書き、ルート直下の素材は削除、`Contents.json` 無変更で `v0.1.20` として TestFlight 配布。Akira さん実機で当該アイコンを確認したうえで再差し替えを判断 → 後継 TODO「アプリアイコンの再差し替え (`photorans-icon.png` 版)」に引継ぎ
- 2026-05-03: グループ詳細の初期セグメントを `グループ` に変更 ([plan archive](docs/plans/archive/group-default-segment.md)) — TestFlight v0.1.18 で発覚した「グループタブから子グループをタップして遷移すると、遷移先 `HomeView` が `未分類` タブから始まり操作の連続性が断ち切られる」事象を `v0.1.20` で修正。原因は `HomeView.selectedSegment` の `@State` 初期値が `.unclassified` 固定で、`GroupDetailView` から push されるたびに新インスタンスが生成され初期値で開始していたこと。`SegmentScope` に `var defaultSegment: HomeSegment` を追加し `.root → .unclassified` / `.group → .groups` で分岐、`HomeView` に明示 `init` を追加し `_selectedSegment = State(initialValue: scope.defaultSegment)` で scope ベース初期化に切替 (named-argument API は維持し `RootView` / `GroupDetailView` 呼び出し側は無変更)。Root の起動 / 復帰時挙動は `.unclassified` 既定のまま、Group 詳細のみ `.groups` で開く。親→子のセグメント引き継ぎは行わない方針で確定 (`NavigationStack` destination 引数として `ItemGroup` しか渡せず Binding 配線が複雑化、かつ Group 詳細への遷移経路は「グループタブ → サブグループタップ」の 1 経路に限定されるため親状態を引き継ぐ動機が薄い)。リーフ Group の空状態 (子グループなし) は MVP では受容、体感が悪ければ Option C (子グループの有無で既定切替) を後追い検討。Akira さん実機確認 OK (2026-05-03 / v0.1.20)
- 2026-05-03: ナビバー削除 + パンくずリンク ([plan archive](docs/plans/archive/breadcrumb-navigation.md)) — Group 詳細の標準ナビバーを 0pt に潰し、Picker 直下の 1 行に「[←] 親 › 子 › [現在地] [⋯]」を統合する UI を `v0.1.19` で実装。Phase 0 で `BreadcrumbView.swift` (祖先列 `ancestorChain` + pop 段数 `popCount` + 描画) と `BreadcrumbPathTests.swift` (chain 4 + popCount 3 = 7 ケース) を新規追加、`ItemGroup.parent` 自己参照ループに対する 64 段打ち切りを `ancestorChain` で担保。Phase 1 で `.searchable` + `searchText` 経路を全削除 (`HomeView` / `GroupListView` / `UnclassifiedListView` / `HomeQueries` から引数除去 + `descendantGroups` / `collectDescendants` 削除)、`SegmentQueryTests` を 5 ケース残し / 削除 8 ケースは末尾の `// MARK: - Removed (search UI). Re-add per TODO「検索 UI 再導入」` コメントブロックで仕様 (S14 系) を保管、再導入 TODO を起票。Phase 2 で `RootView` に `@State path = NavigationPath()` を導入 → `NavigationStack(path:)` 化、`.toolbar(.hidden, for: .navigationBar)` を Root + GroupDetail に当ててナビバー領域を 0pt 化。Phase 3 で `HomeView` に `path: Binding<NavigationPath>?` を追加し Picker 直下にパンくずを条件付き描画 (`.group` scope + `.groups` タブ + path != nil の三条件)、パンくず中間タップは `BreadcrumbView.popCount` 純関数を経由して `path.wrappedValue.removeLast(k)` を呼ぶ結線。Phase 4 で `ViewThatFits(in: .horizontal)` + `ForEach(0..<chain.count)` で左側 (Root 寄り) を `...` に畳むバリアント並列を実装。`v0.1.17` 実機検証で **(1) [未分類|グループ] の並びが逆だった (`HomeSegment` の宣言順 = `Picker` 表示順) → unclassified を先頭に入替 (`v0.1.18`)、(2) 子グループに入ると navbar 風の行が画面上部に出る** 事象が発覚し、後者は調査の結果 Phase 2 で追加した「カスタム上部行 (chevron.left + ellipsis.circle メニュー)」自体が不要だったと判明。`v0.1.19` で customTopBar を削除し、戻るボタンとメニュー (名前を編集 / グループ削除) を **パンくず行と同一の HStack** に統合 (`[←] 親 › 子 › [現在地] [⋯]`)、未分類タブでは行ごと出さない (戻る = edge swipe / メニュー = グループタブ経由) 仕様で確定。アクセシビリティラベル (`戻る` / `「\(name)」へ移動` / `グループ メニュー` / chevron.right `.accessibilityHidden(true)` / 末尾 Text `.accessibilityAddTraits(.isHeader)`) と `.toolbar(.hidden, for: .navigationBar)` + `.toolbarBackground(.hidden, for: .navigationBar)` の二重宣言 (HomeView + GroupDetailView) を維持。Akira さん実機確認 OK (2026-05-03)
- 2026-05-03: ホーム画面 H4 実装 ([plan archive](docs/plans/archive/home-screen-h4-impl.md)) — H4 (NavigationStack 単一 + Root と Group 詳細を同一 `HomeView` で兼用 + 右下 FAB) を `v0.1.16` で完成。Phase1 で旧 `HistoryEntry` を破棄し新スキーマ `Item` + `ItemGroup` + `ItemStatus` (`@Relationship(deleteRule: .cascade)`) を `Storage/` 配下に敷設、`StoreBootstrap.makeContainer()` で `UserDefaults.didMigrateFromHistoryEntryV1` ベースの初回限定フォールバック (旧 schema 検出時のみ store + `Documents/photos` を破棄、フラグ true 後の I/O エラーは `fatalError` で誤発動防止) を実装。Phase2 で `TabView` を撤去し `RootView` の `NavigationStack` 直下に `.navigationDestination(for: ItemGroup.self) / Item.self` を集約 (Phase 0 PoC Step 0.3)、`HomeView(scope:)` を Root / Group 詳細で兼用しセグメント `[グループ | 未分類]` (デフォルト未分類) を `Picker(.segmented)` で実装、`UnclassifiedListView` / `GroupListView` は SwiftData iOS 17 の `#Predicate` で nil 比較が NG (Phase 0 PoC Step 0.1) なので `@Query` 全件 + in-memory filter + sort に統一、`HomeFAB` でカメラ FAB + Group 作成 FAB を縦積み (アクセシビリティラベル + 中心間距離 ≥ 72pt の押し間違い対策つき)。Phase3 で `actor TranslationCoordinator(container: ModelContainer)` を `PhotoransApp` の `@State` で 1 インスタンス保持し environment 経由で配布、`enqueue(itemID:jpegData:)` / `retry(itemID:)` を `PersistentIdentifier` 越境で公開 (`ModelContext` は actor 越境禁止 — Phase 0 PoC Step 0.2)、撮影 → 即 dismiss + `.processing` Item 即挿入 → 背景翻訳 → inplace 更新の楽観的 UI を実装。`ShimmerOverlay` で処理中表示、`Item.retryCount` (上限 3) と「画像ファイルが見つかりません」固定文言で無限リトライを抑止。Phase4 で `ItemDetailView` (削除 + 「グループへ移動」ツールバー)、`MoveToGroupSheet` (全 Group のフラットリスト + 「未分類に移動」)、`GroupRenameSheet` / `GroupDetailView` ツールバーの削除 (子有無で確認文言を分岐)、`ItemGroup.deleteRecursively(modelContext:)` 純関数で写真ファイル → SwiftData 削除の順で再帰削除。Phase5 で `HomeView` 1 箇所に `.searchable` を集約 (子 View では宣言しない、二重配置による点滅対策)、Item は全件横断 + `.completed` のみ、Group は現在 scope 配下のみ、`HomeQueries` 純関数 + `PendingItemRecovery.runIfNeeded` で起動時に `.processing` Item を `coordinator.retry` 経路に流すリカバリ。テストは `StoreBootstrapTests` / `SegmentQueryTests` / `CaptureContextTests` / `TranslationCoordinatorTests` / `ItemGroupDeleteRecursivelyTests` / `PendingItemRecoveryTests` の 6 ケース。Phase6 の TestFlight タグ運用は **`v0.1.14` archive 失敗 (XcodeGen 未実行で `.xcodeproj` に Phase 1〜5 の本体 8 + テスト 5 ファイルが未登録) → `v0.1.15` archive 失敗 (Phase 0 PoC で書いた `ctx[id, as: Item.self]` subscript が SwiftData iOS 17 に実在しなかった) → `v0.1.16` で `xcodegen generate` 再生成 + `TranslationCoordinator.fetchItem(_:in:)` を `FetchDescriptor` 全件 + 線形 filter に書き直して archive 成功**、TestFlight 実機で旧履歴消滅 / セグメント切替 / 楽観的 UI / kill 復帰 / Group 階層化 / 検索 / 削除挙動を Akira さんが確認 (2026-05-03)。再発防止として memory に「`.swift` 追加削除は同 commit に xcodegen 再生成」「WSL2 環境では Swift / SwiftData API シグネチャを文献だけで確定させない」を保存
- 2026-05-02: アプリ画面構成の設計 ([plan archive](docs/plans/archive/screen-architecture.md), [spec archive](docs/plans/archive/home-screen-h4-spec.md), [impl plan](docs/plans/home-screen-h4-impl.md)) — photorans のアプリ画面構成 (写真 + 原文 + 訳文を 1 コンテンツ単位とし、Group ネストでフォルダ化、各画面の保存先解決) を Phase1〜4 で確定。Phase1 でホーム画面構成パターン H1〜H4 を比較プラン化、Phase2 で **H4 (NavigationStack root + Root と Group 詳細を同一 `HomeView` で兼用 + 右下 FAB)** を採用 (Group 階層の任意ネスト + シングル UI 維持の両立が H4 のみで成立)。Phase3 で S1〜S14 の挙動仕様を 5 弾に分けて確定: セグメント構成 = `[グループ | 未分類]` デフォルト未分類 (S1/S2)、データモデル = `Item` + `ItemGroup` + `ItemStatus` + `@Relationship(deleteRule: .cascade)` (S9)、保存先解決 = 現在表示階層 (S4/S13-4/S13-5)、撮影フロー = 楽観的 UI (撮影直後 dismiss + `.processing` Item 即挿入 + 背景翻訳) (S6)、マイグレーション = 旧 `HistoryEntry` ストアと `Documents/photos` を破棄 + `UserDefaults` フラグで初回のみ (S10)、空状態 (S11)、Group 階層化 (Q1) と検索 (Q5/.searchable ベース、Item 全件横断 + Group 名は現在階層配下) (S12-14)。Phase4 で実装プラン `home-screen-h4-impl.md` を起票し、Phase 0 (PoC) + Phase 1 (データモデル) + Phase 2 (UI 骨格) + Phase 3 (楽観的撮影) + Phase 4 (CRUD) + Phase 5 (検索 + リカバリ) + Phase 6 (TestFlight) の 7 段構成 + 各 Step 単位で記述、リスク 8 項目とテスト 6 ケースを明文化。最後の Phase5「実装タスクを TODO に展開して着手」は **ルート TODO「ホーム画面 H4 実装」に分離して継続**、impl Phase 0 (SwiftData Predicate optional keypath / Background `ModelContext` × Sendable / NavigationStack destination 集約 / deleteRule) はドキュメントベース PoC で事前完了済み (2026-05-02)
- 2026-05-02: カメラズーム実装 ([plan archive](docs/plans/archive/camera-zoom.md)) — photorans カメラ画面に AVFoundation `videoZoomFactor` ベースのピンチズーム + 仮想デバイス自動切替を `v0.1.13` で導入。Phase1 で `CameraSession.configureIfNeeded()` のデバイス取得を `AVCaptureDevice.default(.builtInWideAngleCamera)` から `AVCaptureDevice.DiscoverySession([.builtInTripleCamera, .builtInDualWideCamera, .builtInWideAngleCamera], position: .back)` に置換し、Triple / DualWide なら `isVirtualDevice = true` + `initialZoomFactor = 2.0` (= 純正 1.0x = Wide FOV)、Wide 単独は `1.0` を `configureFocus` 内の `lockForConfiguration` 同一ブロックで適用。Phase2 で `CameraSession` に `setZoomFactor(_:)` / `resetZoomToInitial()` (どちらも `sessionQueue.async` + `lockForConfiguration` で直列化、`device.{min,max}AvailableVideoZoomFactor` で再 clamp、permission denied / 未設定時は no-op) と `ZoomSnapshot { isVirtualDevice, initialFactor, minFactor, maxFactor }` を `onConfigured` callback で MainActor に渡すハンドオフ経路を追加し、`@unchecked Sendable` + `sessionQueue` 専有原則を維持して MainActor から `device` を直接読まない不変条件を確立。`CameraViewModel` には `@Observable` ミラー `zoomFactor` / `maxZoomFactor` / `isVirtualDevice` / `isPinching` / 算出 `displayZoomLabel` (仮想デバイスは `factor / 2` で純正 UI 風表記) と `updateZoom(scale:state:)` を追加し、`.began` で `pinchStartFactor = zoomFactor`、`.changed` で `pinchStartFactor * scale` を `1.0..maxZoomFactor` に clamp して `setZoomFactor` を呼ぶ。Phase3 で `CameraPreviewView` に `UIPinchGestureRecognizer` を追加、`Coordinator.handlePinch` で `recognizer.scale` / `state` を `onPinch` closure に流し、`.ended` / `.cancelled` / `.failed` で `recognizer.scale = 1` リセット (tap 1 finger と pinch 2 finger は UIKit が finger count で排他判定するため `requireToFail` 不要)。`CameraView.previewSection` ZStack に倍率 HUD (`Capsule` + `displayZoomLabel`、ピンチ中 1.0 / 非ピンチ時 0.6 で `easeOut(0.2)` クロスフェード、上端から 16pt) を overlay。Phase4 `v0.1.13` の TestFlight 実機検証で「ズームが正しく動作」を Akira さんが確認 (2026-05-02)。アクセシビリティ (VoiceOver / Switch Control から zoom 不可) と background → foreground 復帰時の zoom 維持は MVP の許容仕様として明文化済み (再要望が出れば後追い)
- 2026-05-02: カメラの機能強化の調査 ([plan archive](docs/plans/archive/camera-zoom-research.md)) — photorans カメラ画面にズーム機能を追加するか / どう実装するかを判断するための調査。Phase1 で AVFoundation の `videoZoomFactor` / `ramp(toVideoZoomFactor:)` / `virtualDeviceSwitchOverVideoZoomFactors` 仕様と、iPhone 11〜16 系の `.builtInTripleCamera` / `.builtInDualWideCamera` / `.builtInWideAngleCamera` 利用可能性 + 純正 UI 表記の対応 (仮想デバイス値 = UltraWide FOV 起点で純正 UI と 2 倍ずれる) を表にまとめ、`AVCaptureDevice.DiscoverySession` ベースのフォールバック戦略 (Triple → DualWide → Wide 単独) を確定。Phase2 で iOS 純正カメラ / Google Lens / Microsoft Lens / DeepL カメラを比較し、OCR 翻訳系では「ピンチのみ」が多数派と確認 ⇒ MVP は **ピンチジェスチャのみ + 倍率 HUD + 仮想デバイス自動切替** に決定 (プリセットボタンは端末別ロジックや「2x が光学かどうか」混乱の問題で見送り)。既存 tap-to-focus との competition は finger count で AVFoundation/UIKit が排他判定するので競合制御不要。Phase3 で OCR 精度影響 (digital zoom は `ImageCompressor` の 2048px キャップとの相互作用で被写体あたり実画素を増やす ⇒ ネット positive、Telephoto は別物理素子で当然 positive)、既存設定との互換性 (`.near` AF / portrait lock / `videoRotationAngle = 90` / 撮影中ズーム変更の競合制御 すべてクリア)、session 再構成コスト (仮想デバイスを最初から入力に入れれば input 差し替えなしで constituent 切替が AVFoundation 内部完結 ⇒ 過去観測の 8 秒ブラックアウトは発生しない、WWDC 2018 Session 405 と整合) を確認、実機検証は Phase4 (実装後 TestFlight) にまとめると決定。Phase4 で「実装する」判断と採用仕様を明文化、`docs/plans/camera-zoom.md` を実装プランとして起票し新 TODO「カメラズーム実装」を追加。実機検証は実装後の TestFlight 1 ラウンドで OCR 精度 (1x/2x/3x/5x 比較) + switchover フレーム drop + 起動時間体感をまとめて確認するルートで温存
- 2026-05-02: 利用トークンと料金を管理画面に表示 ([plan archive](docs/plans/archive/admin-usage-cost.md)) — 画像解像度 (= 入力トークン量) と OCR 精度のトレードオフ判断のため、`/admin` に Anthropic API 利用量と USD 料金を可視化。Phase1 で `history` テーブルに `inputTokens` / `outputTokens` / `cacheCreationInputTokens` / `cacheReadInputTokens` の 4 列を追加 (NULL 許容)、`PRAGMA table_info(history)` で列の有無を判定して欠けていれば `ALTER TABLE ADD COLUMN` を冪等実行する初期化を `db.exec` 直後に挿入、`saveHistory` が `response.usage` を引数で受け INSERT。Phase2 で `server/src/pricing.ts` を新設し `claude-sonnet-4-6` の単価 (input $3 / output $15 / cache write $3.75 / cache read $0.30 per MTok) をハードコード、`calculateCost(model, usage)` で USD を返す (表に無いモデルおよび input/output 両 NULL は `null`)。Phase3 で `renderDetailPage` に「使用トークン: input X / output Y」と「料金: $X.XXXX」を追加、cache 列は 0/NULL でなければ併記、NULL は「-」表示。Phase4 で `renderListPage` のヘッダに全期間 / 当月の集計サマリ (件数・input・output・料金 USD) を追加、各行に料金列を追加。Phase5 疎通確認: 空 DB 起動 → 0件 / $0.0000 / 「履歴はまだありません」、レガシー 7 列 DB → 起動時 ALTER で 4 列追加 + 行保持 + 「-」表示、移行済み DB の再起動冪等性、本番 18 件 (NULL 17 + 新規 1) を `nasiogio-01.jpg` 投入で 19 件に → 件数 +1 / input +2,089 / output +1,183 / 料金 +$0.0240 が一覧サマリと新規詳細ページで完全一致 (検算 2089*$3/1M + 1183*$15/1M = $0.024012 → $0.0240)。iOS クライアントは無変更 (`/translate` のレスポンス JSON 形状は不変)
- 2026-05-02: 横向きの写真を撮りたい / 撮影範囲の WYSIWYG 化 ([plan archive](docs/plans/archive/landscape-capture.md)) — 「枠に映っている範囲 = 撮影される範囲」の WYSIWYG 化と、横持ち撮影 JPEG が世界向きで保存されることを `v0.1.7` 〜 `v0.1.12` で実装。Phase1 で `CameraPreviewView.videoGravity` を `.resizeAspectFill` から `.resizeAspect` に変更し、`CameraView` を GeometryReader で「上 = preview (画面幅 × 4/3)、下 = controls」の 2 段構成に再設計。Phase2 で `CameraViewModel` に `lastValidRotationAngle: CGFloat` (`@Observable`) を導入し、orientation observer は `portrait` / `landscapeLeft` / `landscapeRight` のみを 90 / 0 / 180 にマップ、それ以外 (`portraitUpsideDown` / `faceUp` / `faceDown` / `unknown`) は無視して直前値維持 (机上撮影で `.faceUp` が来ても向きを失わない)。Phase3 は B3 → B2 → B3' と試行錯誤: B3 (UI portrait + preview connection だけ世界向き追従) は preview frame が縦長のまま中身横倒しになり UX 不良、B2 (UI ごと auto-rotation) は AVCaptureSession の再構成で **横持ち / 縦持ち切替に約 8 秒のブラックアウト** が出て photorans の用途に不適と判明し `v0.1.9` で破棄、最終的に **B3' = iOS 純正カメラの portrait lock と同じ動作** (`Info.plist` の `UISupportedInterfaceOrientations` を portrait のみ、preview connection の `videoRotationAngle` を 90° 固定、`CameraSession.capturePhoto(rotationAngle:)` だけ `lastValidRotationAngle` を渡して capture connection 側で世界向きに焼き込む) で確定 (`v0.1.10`)。Phase4 で `HistoryDetailView` の固定 `aspectRatio(3.0/4.0)` を撤去し `.scaledToFit() + .frame(maxWidth: .infinity)` に置換、横長 4:3 撮影でも中央クロップされない (`v0.1.11`)。Phase5 で `AVCapturePhotoOutput` が `connection.videoRotationAngle` を尊重してピクセル単位で世界向きに焼き込んだ JPEG を出すことを実機で確認、`ImageCompressor` への EXIF 正規化追加は不要。Phase6 で Phase2/Phase3 のデバッグ計装 (`debugOverlay` / `debugUpdateCount` / `debugConnectionState` / `onApplyState`) を削除し、`v0.1.12` で portrait 撮影リグレッション + 撮影中の回転 / シャッター連打耐性を実機確認。`HistoryRowView.thumbnail` の実機確認 (旧 Step4-3) は別 TODO に分離。判断履歴は memory `feedback_camera_ui_portrait_only.md` に保存 (横長 preview 要望が再発した場合は再確認)
- 2026-05-01: iOS ネイティブで作り直す ([plan archive](docs/plans/archive/ios-native-rewrite.md)) — Expo + RN + VisionCamera 構成を Swift / SwiftUI / AVFoundation / SwiftData フルネイティブに置換 (Phase1-7 完了)。XcodeGen ベースの `ios/Photorans.xcodeproj` を WSL2 で生成・コミット、Bitrise (Hobby plan, Manual signing) で `primary` (シミュレータ) と `release` (タグ `v*` で `xcode-archive@6` → TestFlight) Workflow を運用。AVFoundation で AVCaptureSession + 近接 AF (`autoFocusRangeRestriction = .near`) + タップ AF + サムネ + EXIF orientation 計算、SwiftData (`HistoryEntry`) で永続化、`URLSession` + multipart 自前構築の `/translate` クライアント、履歴一覧 + 詳細 + スワイプ削除 + 撮影直後の自動タブ遷移を実装。TestFlight 配布で altool が要求した AppIcon Asset Catalog 追加 + Bitrise stack を `osx-xcode-26.4.x` に bump して iOS 26 SDK 必須化に対応 (`v0.1.0`)。LAN URL 検証で Anthropic Vision の 5 MiB 上限超過が発覚、`ImageCompressor` で base64 サイズ前提の段階的圧縮 (長辺 ≤ 2048px → quality 0.8/0.65/0.5、target 1.5 MB raw ≒ 2 MB base64) を実装し `v0.1.3` で実機 OK (`v0.1.1` → `v0.1.3`)。Phase8 (本番 URL 復帰 + 旧 RN クライアント `client/` 撤去 + 旧プラン archive) は本番運用着手時に着手するため未実施でクローズ

## 2026-04

- 2026-04-30: Phase3 ネットワーク層 (`/translate` 連携) (`Photorans/Networking/TranslateAPI.swift` を新設、`actor TranslateAPI` + `URLSession` で multipart/form-data クライアントを実装。`Bundle.main` 経由で Info.plist の `API_BASE_URL` を読み、60s タイムアウト + `LocalizedError` 準拠の日本語メッセージ。ATS は当初プランの `NSAllowsArbitraryLoads`(Debug) から、より制限的な `NSExceptionDomains` 方式に変更し `10.0.1.137` のみ HTTP を許可。`CameraViewModel` に `isTranslating` / `lastResult` を追加して撮影成功後に自動翻訳、暫定 `TranslateResultView` (sheet) に原文/訳文/モデルを表示。Phase4-5 で SwiftData + 履歴 UI に置換予定。WSL2 で `xcodegen generate` 成功し Networking グループが pbxproj に登録された。Bitrise ビルドはコミット → push 後に確認)
- 2026-04-28: vibeboard をリポジトリに取り込み、`run-vibeboard.sh` と運用ルールを整備
- 2026-04-28: CLAUDE.md にプロジェクト概要と未確定項目を整理
- 2026-04-29: Phase1-1 server/ 初期化 (Node.js 22 + TypeScript + Hono の最小セット、`npm run dev` で hello world 確認)
- 2026-04-29: Phase1-2 `POST /translate` エンドポイント (multipart 画像受付 + バリデーション + `{originalText, translatedText, model}` 形式の JSON 返却。Claude 呼び出しは Phase1-3 で実装するためスタブ)
- 2026-04-29: Phase1-3 Claude Sonnet 4.6 呼び出し実装 (Anthropic SDK + base64 画像入力 + structured output で OCR/英→日翻訳。`ANTHROPIC_API_KEY` 環境変数が必要)
- 2026-04-29: Phase1-4 履歴保存 (SQLite) (`better-sqlite3` で `${DATA_DIR}/history.db` を初期化、画像本体は `${DATA_DIR}/images/<uuid>.<ext>` に保存。`/translate` 成功時に `id, createdAt, imagePath, imageMimeType, originalText, translatedText, model` を記録。保存失敗はログ出力のみで応答自体はブロックしない)
- 2026-04-29: Phase1-5 管理画面 `/admin` (`/admin` で新着順の履歴一覧、`/admin/:id` で詳細 (画像 + 原文 + 訳文)、`/admin/:id/image` で画像配信。素の HTML + 内蔵 CSS、UUID バリデーションと HTML エスケープ、不正 / 未登録 ID は 404 を返す)
- 2026-04-29: Phase1-7 疎通確認 (`npm run dev` で `tsx watch --env-file=.env` 経由で起動、`server/.env` から `ANTHROPIC_API_KEY` を読み込み、`server/.env.example` を雛形として追加。`curl -F image=@debug-ss/nasiogio-01.jpg /translate` で 200 (28.7s) → 自然な日本語訳、`/admin` 一覧 / `/admin/:id` 詳細 / `/admin/:id/image` の各 GET、バリデーション系 (image 欠落 400 / 未対応 MIME 400 / 不正 ID 404) まで確認。SQLite と画像ファイルが `server/data/` 以下に生成されることも確認)
- 2026-04-29: Phase2-2 ナビゲーション (Bottom Tabs + Stack) (`@react-navigation/native@7` + `bottom-tabs` + `native-stack` を `expo install` で導入、`react-native-screens@~4.16` / `react-native-safe-area-context@~5.6` を SDK 54 互換版で追加。`client/src/navigation/{RootNavigator,HistoryStack,types}.tsx` で Bottom Tabs (カメラ / 一覧) + 一覧→詳細 Stack を構築、`HistoryStackParamList` でルートパラメータを型付け。`App.tsx` を `SafeAreaProvider` + `NavigationContainer` でラップ。`CameraScreen` / `HistoryListScreen` / `HistoryDetailScreen` のプレースホルダを追加 (一覧→詳細はダミー id でナビゲーション可)、`tsc --noEmit` パス)
- 2026-04-29: Phase2-1 Expo 初期化 (`create-expo-app@3.5.3` の `blank-typescript` テンプレートで `client/` を作成、Expo SDK 54.0.0 / React 19.1 / RN 0.81.5 / TypeScript strict、`expo-dev-client@~6.0.21` を `expo install` で追加、`npm run start/ios/android` を `--dev-client` 付きに変更、`typecheck` スクリプト追加。`tsc --noEmit` パス、`npx expo-doctor` 17/17 通過、`expo start --dev-client` で Metro Bundler が `localhost:19000` で待機することまで確認)
- 2026-04-29: Phase2-3 カメラ画面 (撮影 → API 送信 → ローカル DB 保存) (`react-native-vision-camera` で撮影、`expo-file-system` で写真をドキュメント領域へ移動、`/translate` へ multipart 送信して結果を `expo-sqlite` の `history` テーブル (`id, createdAt, imageUri, originalText, translatedText, model`) に保存。完了後は一覧タブへ自動遷移。`client/src/api/translate.ts` / `client/src/db/database.ts` / `client/src/screens/CameraScreen.tsx` を追加、`app.json` にカメラ権限を設定)
- 2026-04-29: Phase2-4 写真一覧画面 (`expo-sqlite` から新着順) (`HistoryListScreen` を `FlatList` で実装、`useFocusEffect` で画面フォーカス時に `listHistory()` を再読込してカメラタブからの戻りで最新撮影が反映されるように。各行はサムネイル + 撮影日時 + 訳文先頭 2 行で、タップで `HistoryDetail` に遷移。履歴 0 件時は空状態メッセージを表示。`tsc --noEmit` パス)
- 2026-04-29: Phase2-5 詳細画面 (写真 + 原文 + 訳文) (`HistoryDetailScreen` を `useFocusEffect` + `getHistoryById(id)` で再読込しつつ、`ScrollView` 内に画像 (アスペクト比 3:4 の `resizeMode="contain"`) + 撮影日時 + 訳文 + 原文 + モデル名を表示。本文は `selectable` でコピー可、レコード未取得時は「履歴が見つかりません」状態を表示。`tsc --noEmit` パス)
- 2026-04-29: Phase2-6 API クライアント (`EXPO_PUBLIC_API_URL` で LAN IP) (`client/src/api/translate.ts` の `getApiBaseUrl()` から `localhost` フォールバックを削除し、未設定時は明示的にエラーを投げて末尾スラッシュを正規化。`AbortController` で 60s タイムアウトを追加して `AbortError` を「タイムアウト」メッセージに翻案。`client/.env.example` を追加 (LAN IP の指定方法と Metro 再起動の注意書き) し、`client/.gitignore` に `.env` を追加。`npm run typecheck` パス)
- 2026-04-29: Phase3-1 サーバ起動 → LAN から `/translate` と `/admin` 到達 (`server/` で `npm run dev` を起動、`@hono/node-server` のデフォルトで `*:3000` (全インターフェース) で listen することを確認。WSL2 上の Linux PC から `http://localhost:3000/admin` 200、LAN IP `http://10.0.1.137:3000/admin` 200、`POST /translate` の空ボディで 400 「image field is required」を確認。最終的に iPhone の Safari から `http://10.0.1.137:3000/admin` で履歴ページを表示できることを実機確認した)
- 2026-04-30: Native実装のカメラで実現可能だけどReactNative使うと不可能なもの、もしくは逆の調査 ([plan archive](docs/plans/archive/native-vs-rn-camera-research.md)) — `react-native-vision-camera` v5 / `expo-camera` / iOS AVFoundation / Android CameraX を 33 項目で比較。photorans 用途には VC v5 の機能で十分（OCR 用 frame processor / manual exposure / multi-lens / EXIF orientation すべて取得可能）、Native のみ可能な機能は Live Photos / Portrait / Cinematic / Apple ProRes / lens position 直指定 / AF Range Restriction / iPhone15 Pro マクロ自動切替など主にクリエイティブ系か周辺機能。短期は VC を維持、AF 不発は `device.minimumFocusDistance` ベースの自動 zoom で対処、横向きは EXIF orientation 正規化、Release WMO クラッシュは VC 固有なので既存 config plugin で対処。Native 化を検討するトリガー条件を 3 つ整理した
- 2026-04-29: Phase3-2 iPhone Dev Client で撮影 → 一覧 → 詳細まで通す (EAS Build の internal distribution プロファイル (`developmentClient: true`) で Dev Client を初回ビルドし、`eas device:create` の Website フローで UDID を Apple Developer Portal に登録、配布 QR から iPhone にインストール。`com.akiraak.photorans` を iOS / Android 共通の bundleIdentifier / package に確定。`react-native-vision-camera` v5.0.8 は `app.plugin.js` を同梱しておらず `npx expo config` がプラグイン解決時に main entry を読みに行って ESM 拡張子なし import で失敗するため、`app.json` の plugins 配列から該当エントリを外して `ios.infoPlist.NSCameraUsageDescription` を直書きに置換。撮影時に `usePhotoOutput()` のデフォルトが `UHD_4_3 (3024×4032) + quality 0.9 + containerFormat: 'native'` で iPhone は HEIC を 5MB 超で出力し Anthropic API が `image exceeds 5 MB maximum` / `Could not process image` で 502 を返していたのを、`targetResolution: FHD_4_3` / `containerFormat: 'jpeg'` / `quality: 0.8` を指定して解決。実機で 撮影 → 翻訳 → 一覧 → 詳細 → `http://10.0.1.137:3000/admin` 反映まで通った)
