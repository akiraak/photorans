# LAN 内のサーバ (server/) を指す URL。
# 実機 (iPhone) からアクセスするので localhost ではなく Linux PC の LAN IP を指定する。
# 例: ifconfig / ip addr で 192.168.x.x を調べて差し替える。
# 値の変更後は Metro Bundler を再起動 (`npm run start`) しないと反映されない。
EXPO_PUBLIC_API_URL=http://192.168.1.10:3000
